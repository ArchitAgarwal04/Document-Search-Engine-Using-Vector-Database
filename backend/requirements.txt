# FastAPI & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# NOTE: do NOT pin bcrypt<4.0.0 — chromadb>=0.5.0 requires bcrypt>=4.0.1
# passlib works with bcrypt 4.x (emits a deprecation warning but does NOT crash)

# Document Parsing
PyPDF2>=3.0.1
python-docx>=1.1.2

# Embeddings
# IMPORTANT: pin CPU-only torch to avoid downloading 1.7GB of CUDA/NVIDIA packages.
# The --extra-index-url below makes pip prefer the smaller CPU wheel (~200MB).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=3.0.1

# Vector Databases
chromadb>=0.5.0
faiss-cpu>=1.9.0

# Gemini (google-genai = new SDK used in code; google-generativeai kept for compatibility)
google-genai>=0.8.0
google-generativeai>=0.7.2

# Groq (high free-tier limits: 6000 RPM)
groq>=0.9.0

# LangChain
langchain>=0.2.6
langchain-google-genai>=1.0.7
langchain-community>=0.2.6

# Utils
pydantic>=2.10.0
pydantic[email]>=2.10.0   # required for EmailStr used in schemas.py
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
httpx>=0.27.0
numpy>=2.0.0
